---
phase: 01-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - server/routes.ts
  - server/index.ts
  - package.json
autonomous: false
requirements: [INFRA-05]

must_haves:
  truths:
    - "App builds and starts with only environment variables (no Replit sidecar, no local uploads directory)"
    - "No import path references replit_integrations at runtime"
    - "App serves a 200 on /api/health after clean start"
  artifacts:
    - path: "server/routes.ts"
      provides: "Clean routes with no Replit integration imports or /uploads /objects handlers"
    - path: "package.json"
      provides: "Clean dependencies — no @google-cloud/storage, google-auth-library, or memorystore"
  key_links:
    - from: "server/index.ts"
      to: "server/routes.ts"
      via: "registerRoutes import"
      pattern: "registerRoutes"
---

<objective>
Remove all Replit-specific code, clean up dead dependencies, and verify the app is deployable to a PaaS host.

Purpose: After Plans 01-02 replaced the session store and storage layer, dead code remains: the replit_integrations directory, the /uploads and /objects Express handlers, unused packages (@google-cloud/storage, google-auth-library, memorystore). This plan removes all of it and verifies the app builds and starts cleanly with only environment variables.

Output: Clean codebase with no Replit dependencies, verified build, deployment-ready configuration.
</objective>

<execution_context>
@/Users/liamfrazer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamfrazer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- No new interfaces needed — this plan removes dead code and verifies -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Replit integrations and dead Express handlers</name>
  <files>server/routes.ts, server/index.ts, package.json</files>
  <action>
1. Delete the entire `server/replit_integrations/` directory:
   ```bash
   rm -rf server/replit_integrations/
   ```

2. In `server/routes.ts`, remove the Express middleware that serves local files:
   - Remove the `/uploads` static middleware block (around line 1066)
   - Remove the `/objects/*` route handler (around line 1076-1087)
   - Verify no other references to these paths remain

3. Search the entire `server/` directory for any remaining imports from `replit_integrations`:
   ```bash
   grep -r "replit_integrations" server/
   ```
   Remove any found.

4. Uninstall dead packages:
   ```bash
   npm uninstall @google-cloud/storage google-auth-library memorystore
   ```

5. Remove Replit-specific dev dependencies (these cause build warnings on non-Replit hosts):
   ```bash
   npm uninstall @replit/vite-plugin-cartographer @replit/vite-plugin-dev-banner @replit/vite-plugin-runtime-error-modal
   ```

6. Check `vite.config.ts` (or wherever Vite is configured) for references to the removed Replit plugins. Remove those plugin imports and usages.

7. In `server/index.ts`, update the trust proxy comment from "Replit runs behind a proxy" to "Production hosts run behind a proxy" (cosmetic but removes confusion).

8. Verify there are no remaining references to Replit-specific environment variables like `PUBLIC_OBJECT_SEARCH_PATHS` anywhere in the codebase:
   ```bash
   grep -r "PUBLIC_OBJECT_SEARCH_PATHS\|REPLIT" server/ shared/ client/
   ```
   Remove any found.
  </action>
  <verify>
    <automated>cd /Users/liamfrazer/socialmediamanager && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -r "replit_integrations\|objectStorageService\|@google-cloud/storage\|memorystore" server/ package.json 2>/dev/null || echo "No Replit references found"</automated>
  </verify>
  <done>No Replit references in codebase, dead packages uninstalled, /uploads and /objects handlers removed, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and create deployment config</name>
  <files>server/index.ts, .env.example</files>
  <action>
1. Run the full build:
   ```bash
   npm run build
   ```
   This must complete with exit code 0. Fix any build errors before proceeding.

2. Verify the build output exists:
   ```bash
   ls -la dist/index.cjs
   ```

3. Update the `/api/health` endpoint in `server/index.ts` to report infrastructure status:
   ```typescript
   app.get("/api/health", (_req, res) => {
     res.json({
       status: "ok",
       timestamp: new Date().toISOString(),
       session_store: "postgresql",
       storage: "cloudflare-r2",
     });
   });
   ```

4. Create a `.env.example` file at the project root listing all required environment variables for production deployment:
   ```
   DATABASE_URL=postgresql://user:pass@host:5432/dbname
   SESSION_SECRET=generate-with-openssl-rand-base64-32
   R2_ENDPOINT=https://accountid.r2.cloudflarestorage.com
   R2_ACCESS_KEY_ID=your-r2-access-key
   R2_SECRET_ACCESS_KEY=your-r2-secret-key
   R2_BUCKET_NAME=your-bucket-name
   R2_PUBLIC_URL=https://pub-xxx.r2.dev
   OPENAI_API_KEY=your-openai-key
   NODE_ENV=production
   ```

5. Rebuild after health endpoint changes:
   ```bash
   npm run build
   ```
  </action>
  <verify>
    <automated>cd /Users/liamfrazer/socialmediamanager && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits 0, dist/index.cjs exists, .env.example lists all required vars, health endpoint reports postgresql sessions and R2 storage</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Phase 1 infrastructure end-to-end</name>
  <files>none</files>
  <action>
Human verifies the complete Phase 1 infrastructure changes work end-to-end. All automation is complete — this checkpoint confirms functional correctness that requires a running app with real R2 credentials.
  </action>
  <verify>Human confirms all 7 verification steps pass</verify>
  <done>User has approved that sessions persist, uploads go to R2, and existing images load correctly</done>
  <what-built>
Complete Phase 1 infrastructure: PostgreSQL sessions, Cloudflare R2 storage, Replit code removed, clean build.

Changes made:
- server/index.ts: Session store swapped to connect-pg-simple
- server/cloud-storage.ts: New R2 upload/delete/URL module
- server/routes.ts: Upload handler uses R2, objectStorageService removed
- script/migrate-uploads-to-r2.ts: One-time migration script
- Removed: server/replit_integrations/, dead packages, Replit Vite plugins
- Added: .env.example with all required vars
  </what-built>
  <how-to-verify>
1. Set R2 environment variables in your .env file (create R2 bucket first if not done)
2. Start the dev server: `npm run dev`
3. Visit http://localhost:5050/api/health — should return JSON with `session_store: "postgresql"` and `storage: "cloudflare-r2"`
4. Log in to the app — session should persist if you restart the dev server
5. Upload a new photo — the photoUrl in the response should start with your R2_PUBLIC_URL
6. If existing local uploads need migrating, run: `npx tsx script/migrate-uploads-to-r2.ts --dry-run` to preview, then without `--dry-run` to execute
7. After migration, browse existing photos — all images should load from R2 CDN URLs
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `grep -r "replit_integrations" server/` returns nothing
- `grep -r "@google-cloud/storage\|memorystore" package.json` returns nothing
- `/api/health` endpoint reports postgresql and cloudflare-r2
- `.env.example` exists with all required variables
</verification>

<success_criteria>
- App builds and starts with only environment variables — no Replit sidecar needed (INFRA-05)
- All Replit-specific code and packages removed
- .env.example documents all required production environment variables
- User has verified sessions persist and uploads go to R2
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>
